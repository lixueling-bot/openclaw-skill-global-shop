# 数字转盘项目设计文档

本目录是顶级数字转盘（digital transport）项目的设计文档集。

## 设计核心理念

**核心问题**：数字转盘相对 CD 转盘音质差的本质，**不是时钟精度不够、不是数据不对**，而是源端在播放过程中产生的电气活动（电源/地噪、时钟相位调制）和音乐内容相关——这种"音乐相关的污染"通过线缆传到 DAC，调制了 DAC 的模拟参考。

**解决方案**：让源端到 DAC 之间，**只传递"边沿"本身，不传递任何其他东西**。最关键的措施是：

1. **末级孤岛**：把"OCXO + 主时钟重对齐 D-FF + 恒流 CML 驱动 + 变压器隔离"这一小段电路从整机中电气隔离出来，使其无论上游和音乐内容如何变化，对外表现的电气行为完全一致。
2. **主时钟在末级生成、不外传**：消除时钟在传输过程中被污染的可能。
3. **数据预读入 RAM、播放时上游断电**：消除播放期间所有随机 I/O 和 CPU 活动。
4. **galvanic 隔离**：变压器/光纤完全切断地环路。

## 文档索引

| 文件 | 内容 | 状态 |
|------|------|------|
| `output-stage-schematic.md` | 末级输出电路完整原理图（OCXO → CML → 变压器） | ✅ 完成 |
| `output-stage-bom.csv` | 末级电路 BOM 清单 | ✅ 完成 |
| `upstream-data-pipeline.md` | 上游数据流水线（SSD → host → FPGA → PSRAM → 末级） | ✅ 完成 |
| `validator-board.md` | 5×8cm 核心验证板设计（测试整个项目的两个核心假设） | ✅ 完成 |
| `power-system.md` | 整机电源系统设计 | 📝 待写 |
| `chassis-mechanical.md` | 机箱、屏蔽、避震设计 | 📝 待写 |

对应的 KiCad 工程：
- `../../hardware/output-stage/` — 完整末级电路（整机用）
- `../../hardware/validator-board/` — 5×8cm 验证子板（先做这个验证理论）

## 整机系统框图（上游 + 末级）

```
╔═══════════════════════════════════════════════════════════════════════╗
║                      HOST 域（嘈杂区，播放时可断电）                    ║
║                                                                          ║
║   [NVMe SSD] ──→ [i.MX 8M Plus SoM] ←─ [Ethernet] [USB] [WiFi]         ║
║                       │                                                  ║
║                       │ LVDS data (装载期) + SPI control                 ║
║                       ▼                                                  ║
╠══════════════════════│══════════════════════════════════════════════════╣
║       Si8645 ×4  ←   Galvanic 隔离边界  →   (无电气接触)                ║
╠══════════════════════│══════════════════════════════════════════════════╣
║                                                                          ║
║                       ▼                                                  ║
║              [Artix-7 35T FPGA] ─→ [PSRAM 512MB]                        ║
║                       │                                                  ║
║                       │ I2S (BCLK/LRCK/SDATA)                            ║
║                       ▼                                                  ║
║              [Si8645 隔离器]                                             ║
║                       │                                                  ║
╠══════════════════════│══════════════════════════════════════════════════╣
║                  PLAYBACK 域中段 + 末级孤岛                              ║
║                       ▼                                                  ║
║              [NB7L72M D-FF] ← [OCXO 22.5792MHz]  ← 主时钟生成于此       ║
║                       │             │                                    ║
║                       │             └→ (反向) 经 Si8645 送给 FPGA       ║
║                       ▼                                                  ║
║              [SY58025U CML 恒流驱动]                                     ║
║                       │                                                  ║
║              [脉冲变压器 T1/T2]                                          ║
║                       │                                                  ║
║                       ▼                                                  ║
║                  [BNC 75Ω SPDIF]   [XLR 110Ω AES]                       ║
║                       │                  │                              ║
╚═══════════════════════│══════════════════│══════════════════════════════╝
                        ▼                  ▼
                       到外部 DAC
```

**信号流摘要**：

1. **装载期**：host 从 SSD 读 FLAC/DSD 文件 → 解码 → 通过 LVDS（经 Si8645）送给 FPGA → 写入 PSRAM
2. **播放期**：host 进入 suspend → FPGA 按末级 OCXO 节拍从 PSRAM 读 → I2S 输出 → 经 Si8645 进末级 → D-FF 用 OCXO 重对齐 → CML 恒流驱动 → 变压器隔离 → BNC/XLR
3. **关键**：播放期间 host 完全不参与，整机"工作的电路"只剩 FPGA + PSRAM + 末级孤岛


## 设计推导过程

完整的第一性原理推导记录在 conversation history 中。关键结论：

1. **DAC 只需要两样东西**：样本值（内容必须正确）+ 采样时钟（相位必须精确）
2. **这两样东西需求相反**：数据无时序要求，时钟无内容要求 → 必须分离
3. **时钟应在 DAC 端生成**：时钟离开生成点就只会变差，不可能变好
4. **播放期间应"零传输"**：每一比特跨越物理边界都是污染源 → 数据应预先放到接收端
5. **如果必须传输（实用版）**：只传纯粹的"边沿"，不附带任何协议、元数据、共模电流、共享电源

## 当前阶段

聚焦"**末级输出电路**"——即从主时钟 OCXO 到 BNC/XLR 之间的几厘米电路。这是数字转盘和 CD 转盘音质差距的**根因所在**，也是改造现有数字转盘成本最低、见效最快的切入点。
